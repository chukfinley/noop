# flutter_app — Notes

## Running / building
- **DO NOT build or run the app** (no `flutter build web`, `flutter run`, no serving, no browser/Playwright screenshots).
  The user runs and tests it himself on Linux.
- Just make the code changes and `flutter analyze`; the user handles running.
